import 'package:flutter/material.dart';

class DisruptionModelingScreen extends StatelessWidget {
  const DisruptionModelingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Disruption Modeling'),
      ),
      body: const Center(
        child: Text('Disruption Modeling Screen'),
      ),
    );
  }
}
